{
  "name": "debobardeur",
  "version": "1.0.0",
  "private": true,
  "description": "Remove bullshit.",
  "author": "Flavien",
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "dev": "pnpm --recursive --parallel run dev",
    "prod": "pnpm --recursive --parallel run prod",
    "build": "pnpm --recursive --parallel run build",
    "start": "pnpm --recursive --parallel run start",
    "export:tests": "node ./packages/backend/src/notion/notion-export-tests.js",
    "export:rules": "node ./packages/backend/src/notion/notion-export-rules.js"
  },
  "devDependencies": {
    "@vitest/ui": "^0.28.5",
    "vitest": "^0.28.5"
  }
}